 @model List<Product>
@{

    ViewBag.PageTitle = "Product Managment";
    ViewBag.CardTitle = "View Products";
    Layout = "_DashboradLayout";
}


<div>

    <a  href="/Admin/Product/Create" class="btn btn-primary">Create New </a>
</div>
<br />
<table id="productTable" class="table table-hover" width="100%">
    <thead class="text-border">
    <tr>
        <td >Name</td>
        <td>Description</td>
        <td>Price</td>
        <td>Category</td>
        <td>Actions</td>
    </tr>
    </thead>


</table>
@section Scripts{
    <script  src="~/js/Product.js"></script>
}


@*    @foreach(var item in Model)
    {
        <tr>

            <td>@item.Name</td>
            <td>@item.Description</td>
            <td>@item.Price</td>
            <td><img src="\@item.img"></td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id" asp-controller="Product" class="btn btn-primary">Edit</a>
                <a asp-action="Delete" asp-route-id="@item.Id" asp-controller="Product" class="btn btn-danger">Delete</a>

            </td>
        </tr>


    } *@